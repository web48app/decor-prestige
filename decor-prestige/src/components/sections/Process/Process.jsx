import styles from './Process.module.css';

export default function Process() {
  return (
    <section id="proces" className={styles.section}>
      {/* Process — sekcja zostanie zaprojektowana w osobnym prompcie */}
    </section>
  );
}
