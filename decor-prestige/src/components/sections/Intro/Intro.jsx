import styles from './Intro.module.css';

export default function Intro() {
  return (
    <section id="intro" className={styles.section}>
      {/* Intro — sekcja zostanie zaprojektowana w osobnym prompcie */}
    </section>
  );
}
