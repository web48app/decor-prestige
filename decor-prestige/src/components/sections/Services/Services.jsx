import styles from './Services.module.css';

export default function Services() {
  return (
    <section id="oferta" className={styles.section}>
      {/* Services — sekcja zostanie zaprojektowana w osobnym prompcie */}
    </section>
  );
}
