import styles from './Gallery.module.css';

export default function Gallery() {
  return (
    <section id="realizacje" className={styles.section}>
      {/* Gallery — sekcja zostanie zaprojektowana w osobnym prompcie */}
    </section>
  );
}
