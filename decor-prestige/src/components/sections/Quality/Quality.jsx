import styles from './Quality.module.css';

export default function Quality() {
  return (
    <section id="jakosc" className={styles.section}>
      {/* Quality — sekcja zostanie zaprojektowana w osobnym prompcie */}
    </section>
  );
}
