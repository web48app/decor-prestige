import styles from './Hero.module.css';

export default function Hero() {
  return (
    <section id="start" className={styles.section}>
      {/* Hero — sekcja zostanie zaprojektowana w osobnym prompcie */}
    </section>
  );
}
