import styles from './CTA.module.css';

export default function CTA() {
  return (
    <section id="cta" className={styles.section}>
      {/* CTA — sekcja zostanie zaprojektowana w osobnym prompcie */}
    </section>
  );
}
