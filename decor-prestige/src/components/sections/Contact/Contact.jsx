import styles from './Contact.module.css';

export default function Contact() {
  return (
    <section id="kontakt" className={styles.section}>
      {/* Contact — sekcja zostanie zaprojektowana w osobnym prompcie */}
    </section>
  );
}
